import styled from 'styled-components/native'
import tailwind from 'tailwind-rn'

export const DefaultView = styled.View(tailwind('bg-white flex-1 p-2'))
export const ViewTitle = styled.Text(tailwind('font-bold text-xl my-2'))
